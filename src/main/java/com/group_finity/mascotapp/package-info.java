/**
 * Classes to do with all the gross ui/settings code.
 * the mascot package should just be the little character running around on the screen.
 * <p>
 * If these were modules, this package would import mascot but not the other way around.
 * Most of the UI should eventually be moved here (but that'll be a slow process).
 */
package com.group_finity.mascotapp;
